name: CI Pipeline

on:
  push:
    branches:
      - main  # Trigger on push to the main branch (adjust as needed)
  pull_request:
    branches:
      - main  # Trigger on PR to the main branch (adjust as needed)

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      IMAGE_NAME: "angular-contact-app"
      IMAGE_TAG: "latest"
      CONTAINER_NAME: "angular-contact-container"

    steps:
      # Step 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Set up Docker
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # Step 3: Build Angular App
      - name: Build Angular App
        working-directory: ./angular-contact-list
        run: |
          npm install
          npm run build

      # Step 4: Build Docker Image
      - name: Build Docker Image
        run: |
          docker build -t ${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }} .

      # Step 5: Run Trivy Scan
      - name: Run Trivy Scan
        run: |
          docker run --rm -v /var/run/docker.sock:/var/run/docker.sock aquasec/trivy:latest image ${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }} --severity HIGH,CRITICAL --no-progress --format table -o trivy-report.html || true

